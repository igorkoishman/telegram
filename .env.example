# Telegram Bot Configuration
# Get your token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Webhook secret for security
TELEGRAM_WEBHOOK_SECRET=your_random_secret_here

# Cloudflare Tunnel Token
# Get this from Cloudflare Zero Trust Dashboard after creating a tunnel
# See CLOUDFLARE_TUNNEL_SETUP.md for detailed instructions
CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token_here

# Docker Hub username (for pulling images)
DOCKER_USERNAME=igorkoishman

# Java Memory Settings (optional)
# Adjust based on your system resources
JAVA_OPTS=-Xmx4g -Xms1g

# Spring Profile (optional)
SPRING_PROFILES_ACTIVE=production

# Model Auto-Download Configuration
# Set to 'true' to download models on startup, 'false' to download on first use
TRANSLATION_MODELS_AUTO_DOWNLOAD=true

# Whisper Models to Download on Startup
# Options: tiny, base, small, medium, large, large-v2, large-v3
# Default: tiny,small (fast startup, other models download on-demand)
# Comma-separated list for multiple models (e.g., "tiny,small,medium")
TRANSLATION_MODELS_WHISPER_MODELS=tiny,small

# Whisper Backends to Download
# Options: faster-whisper, openai-whisper
# Default: Both backends (for flexibility)
# Comma-separated list - download models for both backends if needed
TRANSLATION_MODELS_WHISPER_BACKENDS=faster-whisper,openai-whisper

# Translation Models to Download on Startup
# Options: m2m100, nllb
# Default: m2m100 (smaller and faster, nllb downloads on-demand if needed)
# Comma-separated list
TRANSLATION_MODELS_TRANSLATION_MODELS=m2m100
